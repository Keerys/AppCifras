#Criar tabela MySQL, código para usar no prompt do mysql
import mysql.connector


create table cifras (
    id INT NOT NULL AUTO_INCREMENT,
    nome VARCHAR(100),
    artista VARCHAR(50),
    tom VARCHAR(10),
    estilo VARCHAR(25),
    PRIMARY KEY (id)
);

#inserir dados na tabela SQL

INSERT INTO produtos (codigo,descricao,preco,categoria) VALUES (123, 'impressora', 500.00, 'informática')